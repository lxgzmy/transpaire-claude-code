# templates/

Sanitised prompt / document / output templates used by this role's workflows and
skills. **No real client data** — use placeholders (e.g. `{{job_no}}`, `{{suburb}}`).

Examples this will hold later: an EOI-extraction prompt template, a variation
summary/wording template, and the evidence-bundle layout for review.
