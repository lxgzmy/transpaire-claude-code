# Reference — Permit Officer

Source material lives outside the repo (it contains client PII); this file
only points at it.

| Source | Where | Notes |
|---|---|---|
| Discovery recording, 20 Aug 2026 | To be filed on `Z:` in the AI testing folder | `AI consultation - Approval-20260820_040605UTC-Meeting Recording.mp4`; the substantive session ends when the guests leave the call. |
| Discovery record (de-identified) | `Z:\CLAUDE CODE\cowork-projects\5. permit_officer\` | 12-page PDF, 20 Aug 2026; the repo's de-identified extraction is `docs/superpowers/brainstorms/2026-08-20-permit-officer-approvals.md`. |
| AI testing folder (per-role) | `Z:\AI test\Permit Officer` | Created by the business owner 21 Aug 2026 and populated 31 Aug: `Standard RFI Tasks .docx` (the standard RFI item vocabulary — also the input to the OSC workflow-enrichment session), `WEEKLY BA REPORT PROCEDURE.pdf` (the report's OSC Executive Reporter export mechanics: saved definition, BA-lodged-jobs filter, Excel formatting steps), `NSW PROCEDURES\` (CDC lodgement, energy report, insurance, principal-certifier appointment, water-authority applications), `QLD PROCEDURES\` (energy report, insurance). |
| Still to drop into the AI testing folder | `Z:\AI test\Permit Officer` | A recent weekly report cycle (OSC export + the officer's final + the marked-up printed copy), past RFIs for the checklist corpus (NSW standard forms prioritised), blank council/certifier application forms. |
| Current weekly BA report | Maintained by the permit officer (Excel, updated Mondays) | Never copy into the repo; a **synthetic** layout fixture may go under `../fixtures/` once the template is agreed. |
| OSC field map | `osc-field-map.md` (this folder) — to be written | De-identified endpoint → report-column mapping from the field-mapping pass against the read-only `osc-api` MCP, plus the list of facts OSC cannot yet provide (feeds the workflow-enrichment ask). |
